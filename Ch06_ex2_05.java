/* Overloading
public void setMonth(int monthNumber)
{
    if (monthOK(monthNumber)) {
        month = monthString(monthNumber);
    } else {
        System.out.println("Fatal Error");
        System.exit(0);
    }
}
*/